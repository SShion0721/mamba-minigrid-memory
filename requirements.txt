# Install a CUDA-enabled PyTorch build first for your CUDA/driver.
# Then install this file with --no-build-isolation so Mamba builds against it:
#   pip install -r requirements.txt --no-build-isolation

numpy>=1.24,<2
gymnasium>=1.3.0
minigrid>=3.1.0
tensorboard>=2.16
tqdm>=4.66
einops>=0.8
imageio[ffmpeg]>=2.34

# PyPI latest verified on 2026-05-11. For the newest GitHub release/source
# build, use the source-install command in README.md.
causal-conv1d>=1.6.1
mamba-ssm>=2.3.1
